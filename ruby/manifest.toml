name = "ruby"
display_name = "Ruby Language Support"
description = "LSP for Ruby"
version = "1.1.0"
file_extensions = [".rb", ".rake", ".gemspec"]
language_ids = ["ruby"]
workspace_markers = ["Gemfile", "Gemfile.lock"]

[lsp]
binary = "ruby-lsp"
install = "gem install ruby-lsp"
install_macos = "brew install ruby-lsp"
args = []
dependencies = ["gem"]

scripts = []
